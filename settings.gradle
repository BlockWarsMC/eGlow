pluginManagement {
    //includeBuild("build-logic")
    repositories {
        gradlePluginPortal()
        maven { url = 'https://jitpack.io' }
        maven { url = "https://repo.papermc.io/repository/maven-public/" } // BW - Add paper repo
        mavenCentral()
    }
}

rootProject.name = "eGlow"